{
  "name": "coa-xml",
  "version": "1.0.2",
  "description": "xml component for coa",
  "keywords": [
    "coajs",
    "coa",
    "xml"
  ],
  "license": "MIT",
  "author": "Aex",
  "homepage": "https://github.com/coajs/coa-xml",
  "repository": {
    "type": "git",
    "url": "https://github.com/coajs/coa-xml.git"
  },
  "scripts": {
    "dev": "tsc -w",
    "build": "rm -rf dist && tsc && cp package.json dist",
    "npm-publish": "yarn build && yarn version --patch && cp package.json README.md dist && cd dist && npm publish"
  },
  "dependencies": {
    "@types/xml2js": "^0.4.8",
    "xml2js": "^0.4.23"
  },
  "devDependencies": {
    "@types/node": "^14.14.35",
    "typescript": "^4.2.3"
  }
}
